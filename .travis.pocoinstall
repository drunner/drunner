#!/bin/bash

POCOVER="poco-1.7.3"

mkdir /poco
cd /poco
wget http://pocoproject.org/releases/${POCOVER}/${POCOVER}.tar.gz
gunzip ${POCOVER}.tar.gz
tar xf ${POCOVER}.tar
cd ${POCOVER}
./configure --static
make -j4
make install

