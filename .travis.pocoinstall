#!/bin/bash

POCOVER="poco-1.7.3"

sudo mkdir /poco
sudo cd /poco
sudo wget http://pocoproject.org/releases/${POCOVER}/${POCOVER}.tar.gz
sudo gunzip ${POCOVER}.tar.gz
sudo tar xf ${POCOVER}.tar
sudo cd ${POCOVER}
sudo ./configure
sudo make -j4
sudo make install

